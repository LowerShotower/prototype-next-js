import { type FunctionComponent, type ReactNode } from 'react'
import { Styled{{name}} } from './{{name}}.styles'

interface {{name}}Props {
  children?: ReactNode
  className?: string
}

const {{name}}: FunctionComponent<{{name}}Props> = ({ className, children }) => {
  return <Styled{{name}} className={className}>{children}</Styled{{name}}>
}

export default {{name}}
